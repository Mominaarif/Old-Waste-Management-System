import React from 'react'

export default function redux() {
    //  Store
    {
        count: {value: 0}
        isLoggedIn: {value: false}
    }
    state.count.value;
    state.isLoggedIn.value;

    //  Actions
    const increment = {type: 'INCREMENT', payloas: 10};

    //Reducers

    // Action --> Reducer --> Change state
  return (
    <div>redux</div>
  )
}
